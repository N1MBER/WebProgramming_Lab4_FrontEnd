const initialState = {
    secondName: "Kolesnikov",
    firstName: "Maxim",
    variant: 631214,
    topic: "Laboratory work №4"
};

export function headerReducer(state = initialState) {
    return state;
}